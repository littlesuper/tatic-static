# tatic-static

GitHub Pages site for `tatic-gh.awer.cc`. It exists to serve
`/.well-known/apple-app-site-association` (Apple Associated Domains, `webcredentials`)
for the 默（Tacit）passkey feasibility spike. `.nojekyll` keeps the dot-directory published.
